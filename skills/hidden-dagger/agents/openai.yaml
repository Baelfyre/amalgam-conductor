interface:
  display_name: "Hidden Dagger"
  short_description: "Run gated adversarial resilience testing"
  default_prompt: "Use $hidden-dagger to create a safety-gated negative and destructive testing plan without executing risky tests."
policy:
  allow_implicit_invocation: false
