# Local AI Installation Guide

## Manual prompt loading
Paste one selected `SKILL.md` after the system instructions.

## Retrieval method
Index root guidance and skill files with purpose and activation metadata.

## Folder knowledge method
Expose the repository as a read-only knowledge folder when the runtime supports it.

## Minimal context method
Load Amalgam Conductor's routing summary, then one specialist and only required references.

## Offline use
All core instructions are Markdown and require no network access. Tool execution still depends on the local runtime.
